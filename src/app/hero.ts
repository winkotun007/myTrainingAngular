export class Hero{
    id? : number;
    name? : string;

    constructor() {
        this.name =''; // Initialize name with a default value
        this.id = 0;   // Initialize number with a default value
      }
}